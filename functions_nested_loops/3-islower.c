#include "main.h"
/**
 * _islower - Output 1 if char is c if not it will print 0
 * c - will hold an int
 * Return: 0 success
 *
 */


int _islower(int c)
{
if (c == 97)
	return (1);
else
	return (0);
}

